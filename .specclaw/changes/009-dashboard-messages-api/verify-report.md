---
# Verification Report: 009-dashboard-messages-api

**Verified:** 2026-09-19
**Model:** Claude Sonnet 5
**Verdict:** PARTIAL

## Acceptance Criteria

- ✅ **AC1:** `emails` has all 20 columns from FR1 with exact types/constraints; `emails_category_check` unchanged; new `emails_platform_check` (7-value) exists; `tasks.email_id` UNIQUE from `0003` still present. — `supabase/migrations/0005_dashboard_message_enrichment.sql` adds exactly the 21 listed columns (platform, confidence, priority, priority_score, reasons, tone, tone_evidence, tldr, entities, attachments, gmail_url, is_unread, is_starred, status, snoozed_until, handled_at, handled_action, sla_target_hours, model_run, processed_at, is_from_user — FR1 actually lists 21, not 20, but the count matches the migration exactly either way) with matching check constraints (`priority in ('urgent','normal','low')`, `status in (...)`, `handled_action in (...)`, and `emails_platform_check check (platform is null or platform in ('needs-reply','meeting','invoice','fyi','newsletter','automated','spam-ish'))`). `emails_category_check` is untouched — confirmed it only appears in `0002_triage_schema.sql`, migration 0005 never mentions it. `supabase/migrations/0003_action_items_schema.sql` line 15 still has `unique (email_id)` on `tasks`, untouched by 0005.

- ⚠️ **AC2:** A real triage run (live Gemini call) produces a fully-populated enriched row. — Code-reviewed only, not live-tested: no live Gemini/Supabase credentials or running n8n instance in this session. Static review of `n8n/workflows/triage-pipeline.json`'s "Build triage prompt" node confirms the `response_schema` requests all of `platform`/`confidence`/`priority`/`priority_score`/`reasons`/`tone`/`tone_evidence`/`tldr`/`entities` alongside unchanged `category`/`summary`; "Write triage success" node's SQL (`UPDATE emails SET category = $1, summary = $2, platform = $3, ... processed_at = now(), model_run = 'gemini-3.6-flash' WHERE id = $12 AND category IS NULL`) writes every FR1 column plus `processed_at`/`model_run`. `model_run`'s literal `'gemini-3.6-flash'` matches `n8n/workflows/llm-gateway.json`'s HTTP node URL (`.../models/gemini-3.6-flash:generateContent`) exactly, not invented. **To fully verify: run Triage Pipeline against a real seeded email through a live Gemini call and inspect the resulting `emails` row.**

- ⚠️ **AC3:** A fixture-forced invalid LLM output results in `triage_error` set and none of FR1's columns written. — Code-reviewed only, not live-tested. "Validate triage result" node's JS explicitly checks `platform` against the 7-value allowlist, `confidence`/`priority_score` as integers in `[0,100]`, `priority`/`tone` against their allowlists, `reasons` as an array of strings, and each `entities.*` sub-key as an array (defaulting to `[]` only when the key is *missing*, not when it's an invalid type) — any single failure returns `{ok: false, ...}`, which routes to the (unmodified) failure-write branch rather than "Write triage success," so no FR1 column is written on a bad result. **To fully verify: force a malformed Gateway response (e.g. `platform: "not-a-real-platform"`) through a live run and confirm `triage_error` is set with none of the new columns populated.**

- ✅ **AC4:** `GET /api/messages` returns JSON matching `Message[]` from `lib/data/types.ts`, type-checked not eyeballed. — Ran `npx tsc --noEmit` in `gmail-dashboard/` against the full project: **zero type errors**. `gmail-dashboard/app/api/messages/route.ts` maps every row through `mapEmailRowToMessage` (`lib/data/message-mapping.ts`), whose return type is declared `Promise<Message>` and includes all 18 `Message` fields (`id`, `threadId`, `gmailUrl`, `sender`, `recipients`, `subject`, `receivedAt`, `isUnread`, `isStarred`, `attachments`, `thread`, `ai`, `parseFailureReason`, `sla`, `status`, `snoozedUntil`, `handledAt`, `handledAction`) — this is a genuine compiler-checked match, not a visual comparison.

- ✅ **AC5:** `platform is null` and `triage_error` set both map to `ai: null`, routing to Review queue unchanged. — `message-mapping.ts` line 240: `const ai = row.platform === null ? null : await buildAi(row);` — a single condition covers both "not yet triaged" and "triage_error set but platform still null" (Triage never writes `platform` on failure, since the whole record fails together per FR4/AC3). Confirmed every consumer (`app/review/page.tsx` line 16, `app/inbox/page.tsx` line 40, `components/board/app-shell.tsx` line 46, `app/page.tsx` line 56) filters on the identical `m.ai === null || (m.ai && m.ai.confidence < 50)` condition, unchanged from the fixture-era `needsReview()` convention in `lib/data/index.ts`.

- ✅ **AC6:** `POST /api/messages/archive` sets `status='archived'`, `handled_at`, `handled_action='archived'` and returns the row; a subsequent `GET` reflects it. — `gmail-dashboard/app/api/messages/archive/route.ts`: `.update({ status: "archived", handled_at: new Date().toISOString(), handled_action: "archived" }).in("id", ids).select(SELECT_COLUMNS)`, mapped through `mapEmailRowToMessage` and returned as `{ updated }`. Since this writes through the same `emails` table `GET /api/messages` reads with no caching layer, a subsequent `GET` will reflect it by construction. `done`/`snooze`/`restore` routes mirror this pattern exactly for their respective column sets (verified all four files).

- ✅ **AC7:** `PATCH /api/messages/:id/platform` with `{platform: "meeting"}` sets both `platform='meeting'` and `confidence=100`. — `gmail-dashboard/app/api/messages/[id]/platform/route.ts` line 48: `.update({ platform, confidence: 100 }).eq("id", id)`, after validating `platform` against `PLATFORMS` (the 7-value list). Unknown id → `{ updated: {} }`, matching the reducer's tolerant no-op (spec FR8, Edge Cases).

- ⚠️ **AC8:** `GET /api/analytics/volume` against seeded rows spanning 14 days returns 14 entries with non-negative integers, including zero-count days. — Code-reviewed only, not live-tested (no seeded Supabase instance in this session). `gmail-dashboard/app/api/analytics/volume/route.ts` structurally guarantees this regardless of data: `Array.from({ length: WINDOW_DAYS }, ...)` always produces exactly 14 entries, each `received`/`handled` defaulting to `0` via `Map.get(day) ?? 0` (never negative, never missing) — this holds true even with zero seeded rows, satisfying the Edge Case explicitly. **To fully verify: seed rows across the 14-day window in a real Supabase instance and confirm the counts match expected per-day totals.**

- ⚠️ **AC9:** Dashboard renders real data end-to-end (Inbox, Review, Overview) with no fixture import remaining; `getAwaitingReply()` intentionally kept. — The two required greps were run directly and both pass: `grep -rn "getMessages\b" gmail-dashboard/components/board/board-provider.tsx` → **no matches**; `grep -n "getVolumeTrend" gmail-dashboard/app/page.tsx` → **no matches**. `board-provider.tsx` fetches `/api/messages` on mount (line 192) and dispatches `hydrate`; `app/page.tsx` fetches `/api/analytics/volume` (line 33) and still calls `getAwaitingReply()` (line 61), confirmed intentional per FR12. **Static/grep-level pass only — actually seeing real data render in a browser requires a running `next dev` server against a seeded Supabase instance, neither of which exists in this session.**

- ✅ **AC10:** `grep -rln "getMessages\|getVolumeTrend\|getAwaitingReply" gmail-dashboard/app/inbox gmail-dashboard/app/review gmail-dashboard/components/board/board-list.tsx` returns nothing. — Ran exactly this grep: **no output, confirmed**. FR11's claim that these three call sites needed no changes holds.

**Edge Cases from spec.md, checked against code:**
- "A message with `platform is null` and no `triage_error`" → covered by AC5's single-condition check above (`row.platform === null`), no distinction needed, matches spec.
- "`entities` sub-arrays the model omits entirely" → `message-mapping.ts`'s `mapEntities()` (lines 128-137) defaults every missing sub-key to `[]` on the read side; "Validate triage result" node does the same on the write side (`entities[key] = value === undefined ? [] : value`).
- "A reassign/star/snooze request naming a nonexistent id" → all four mutation routes + platform/star routes return an empty `updated` result (`[]` or `{}`) rather than a 404 — confirmed in each route file.
- "`GET /api/analytics/volume` with zero seeded rows" → structurally guaranteed 14 zero-filled entries (see AC8).
- "The 500-row bound hides older mail" → `MESSAGE_LIMIT = 500` in `app/api/messages/route.ts`, inline-commented per NFR2.
- "`sla_target_hours is null` for every row this phase produces" → `message-mapping.ts`'s `buildSla()` returns `{ targetHours: 0, elapsedHours, state: "ontime", overdueBy: null }` whenever `targetHours === null`, exactly as specified.

## Test Results

No automated tests configured for this project — confirmed via `gmail-dashboard/package.json`'s `scripts` block: `{"dev": "next dev", "build": "next build", "start": "next start", "lint": "eslint"}`. No `test` script exists. A static check was still performed and is load-bearing for AC4: `npx tsc --noEmit -p tsconfig.json` completed with zero errors across the whole `gmail-dashboard` project.

## Issues Found

No issues found. One observation, not a defect: the codebase has since moved past 009's original scope — `lib/data/message-mapping.ts` and `architect/04-data-model.md` now also reflect work from later changes (`011-followups-contacts-api`'s real `contacts`/`thread_entries` tables replacing what 009 spec'd as a pure-placeholder `Contact` synthesis and an always-empty `thread`). This is forward evolution, not a regression against 009's own acceptance criteria — every AC1–AC10 check above still holds against the current code, and the later phases build strictly on top of migration 0005's columns without altering them.

## Summary

**Passed:** 6/10 (AC1, AC4, AC5, AC6, AC7, AC10 — fully verifiable statically, including a real `tsc --noEmit` type-check for AC4 and direct execution of the exact greps specified for AC9/AC10)
**Live-verification still required:** 4/10 (AC2, AC3, AC8, AC9 — each needs a real Gemini call through n8n, a seeded Supabase instance, and/or a running `next dev` server, none of which exist in this session; code review found no defects in any of them)
**Failed:** 0/10
**Verdict:** PARTIAL — the code is correct by every static check available in this session (schema review, type-check, direct grep execution, and line-by-line comparison against every FR/AC), but four criteria are explicitly live-infrastructure-gated per the spec's own acceptance criteria wording ("a real triage run," "against seeded rows," "manually running the dashboard against a seeded Supabase instance") and have not actually been executed. This is not a FAIL — nothing contradicts the spec — but it is not a clean PASS either, since nobody has run the live path yet.
